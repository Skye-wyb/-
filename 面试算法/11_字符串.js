let str = '1423', k = 3, n = str.length

function getAll (str) {
    let ans = []
    function dfs (temp) {
        ans.push(temp.join(''))
        for (let i = 0; i < str.length; i++) { }
    }
}